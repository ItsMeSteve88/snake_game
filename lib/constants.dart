import 'package:flutter/material.dart';

// COLORS

const backgroundColor = Colors.amber;
const snakeColor = Colors.white;
const foodColor = Colors.red;
const buttonColor = Colors.blue;
const textColor = Colors.white;
